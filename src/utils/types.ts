export type TJMap = Record<string, any>;
export type TJMapList = Array<TJMap>;

/* ------------------------------------------------------------------------------------------------ */

export type TPerson = {
  id?: number,
  name: string,
  number: string,
}

export type TPersonList = Array<TPerson>;
